{% if stations | length %} 
        {% for station in stations %}
                       <pre>
                        {{ dump(station) }}
                    </pre>
         {% endfor %}
 {% endif %}