 
<form action="#" class="form-horizontal" id="idForm_filtrer_periode" method="post">
   <table class="table table-bordered table-condensed table-advance" id="idTable_periodes">
       <thead>
           <tr>
               <th width="1%" align="center">
                   <a class="btn btn-success" id="cocherToutLesPeriodes"  href="#"><i class="fa fa-check fa-lg"></i></a>
               </th>
               <th width="5%">Périodes</th>
               <th>Intervalles</th>
           </tr>
       </thead>
       <tbody id="idBody_filtrer_periodes">
                {%if periodes | length%} 
                 {% for periode in periodes %}
                     <tr>
                         <td>
                             <div class="checbox">
                                 {% if periode.sel == 'O' %} 
                                 <input type="checkbox" id="checkPeriode_{{  periode.periode.id }}" name="checkPeriode[]" value="{{  periode.periode.id }}" checked="checked" >
                                 {% else %}
                                  <input type="checkbox" id="checkPeriode_{{  periode.periode.id }}" name="checkPeriode[]" value="{{  periode.periode.id }}" >
                                 {% endif %}
                             </div>
                         </td> 
                         <td nowrap>{{ periode.periode.labelPeriode }}</td>
                         <td nowrap>du {{ periode.periode.dateDeb | date("d/m/Y") }}&nbsp;&nbsp;au&nbsp;&nbsp; {{ periode.periode.dateFin | date("d/m/Y") }}</td>
                     </tr>
                 {% endfor %}
             {% endif %}
        </tbody>
   </table>
  </form>

