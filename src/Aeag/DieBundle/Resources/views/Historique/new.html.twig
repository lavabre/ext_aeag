<h1>Historique creation</h1>

<form action="{{ path('historique_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('historique') }}">
            Back to the list
        </a>
    </li>
</ul>
